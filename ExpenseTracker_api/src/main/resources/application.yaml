spring:
  application:
    name: ExpenseTracker_api
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&preparedStatementCacheQueries=0
    username: postgres.uujzrwioggprwhntxsag
    password: expensetracker2026
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: expensetracker.smtp@gmail.com
    password: kzwkrfppbslhiecb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

ocr:
  space:
    api-key: "K88857534688957"
    url: "https://api.ocr.space/parse/image"

server:
  port: ${PORT:8080}
