spring:
  application:
    name: ExpenseTracker_api

  datasource:
    url: jdbc:postgresql://db.uujzrwioggprwhntxsag.supabase.co:5432/postgres
    username: postgres
    password: expensetracker2026
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080